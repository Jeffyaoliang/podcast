# DreamEcho Android 客户端运行指南

## 一、环境准备

### 1.1 必备软件

| 软件 | 版本要求 | 下载地址 |
|------|----------|----------|
| Android Studio | 2023.1+ | https://developer.android.com/studio |
| JDK | 17+ | https://www.oracle.com/java/technologies/downloads/ |
| Go 后端 | 1.21+ | https://go.dev/dl/ |

### 1.2 系统要求

- **操作系统**: Windows 10/11
- **内存**: 8GB+（推荐 16GB）
- **硬盘**: 10GB+ 可用空间
- **处理器**: 支持虚拟化（VT-x）

---

## 二、安装步骤

### 2.1 安装 Android Studio

1. 访问官网下载：https://developer.android.com/studio
2. 运行安装程序
3. 选择安装组件：
   - ✅ Android SDK
   - ✅ Android SDK Platform
   - ✅ Performance (Intel® HAXM)
   - ✅ Android Virtual Device
4. 完成安装

### 2.2 配置 Android SDK

安装完成后，启动 Android Studio，首次启动会自动下载 SDK。如果需要手动配置：

1. 打开 Android Studio
2. 点击 **More Actions** → **SDK Manager**
3. 在 **SDK Platforms** 标签页中，选择：
   - ✅ Android 14 (API 34)
4. 在 **SDK Tools** 标签页中，确保以下工具已安装：
   - ✅ Android SDK Build-Tools
   - ✅ Android SDK Platform-Tools
   - ✅ Android SDK Tools
5. 点击 **Apply** 下载安装

### 2.3 安装 JDK 17

如果未安装 JDK 17：

1. 访问：https://www.oracle.com/java/technologies/downloads/
2. 下载 **JDK 17 (LTS)** for Windows
3. 运行安装程序
4. 配置环境变量：
   ```
   JAVA_HOME = C:\Program Files\Java\jdk-17
   PATH = %JAVA_HOME%\bin
   ```

---

## 三、项目配置

### 3.1 检查 Go 后端

确保 Go 后端已配置你的电脑 IP 地址：

```kotlin:android/app/src/main/kotlin/com/dreamecho/app/network/ApiService.kt:44
private const val BASE_URL = "http://192.168.153.1:8080/"
```

如果你的 IP 地址不是 `192.168.153.1`，需要修改。

获取当前 IP 地址：
```cmd
ipconfig | findstr "IPv4"
```

### 3.2 检查项目结构

确认以下文件存在：

```
E:\hack\minimax\android\
├── app\
│   ├── build.gradle.kts
│   └── src\main\
│       ├── AndroidManifest.xml
│       └── kotlin\com\dreamecho\app\
│           ├── MainActivity.kt
│           ├── models\
│           │   └── Podcast.kt
│           ├── network\
│           │   └── ApiService.kt
│           └── ui\
│               ├── screens\
│               │   ├── HomeScreen.kt
│               │   ├── LoginScreen.kt
│               │   ├── SearchScreen.kt
│               │   ├── SubscriptionsScreen.kt
│               │   └── ProfileScreen.kt
│               └── theme\
│                   ├── Color.kt
│                   ├── Theme.kt
│                   └── Type.kt
```

---

## 四、运行项目

### 4.1 打开项目

1. 启动 Android Studio
2. 点击 **Open** 或 **File → Open**
3. 选择文件夹：`E:\hack\minimax\android`
4. 等待 Gradle 同步完成（可能需要几分钟下载依赖）

### 4.2 创建模拟器

1. 在 Android Studio 中，点击 **Device Manager**
2. 点击 **Create device**
3. 选择设备：
   - **Category**: Phone
   - **Name**: Pixel 7
   - **Resolution**: 1080 x 2400
4. 选择系统镜像：
   - **Release Name**: Android 14 (UpsideDownCake)
   - **API Level**: 34
   - **Target**: Android 14 (Google Play)
5. 点击 **Finish** 创建

### 4.3 启动 Go 后端

在新终端中启动 Go 后端：

```cmd
cd E:\hack\minimax\backend
go run main.go
```

看到以下输出表示启动成功：
```
DreamEcho Backend server running on http://localhost:8080
API Endpoints:
  - POST /api/login
  - GET  /api/feed?url=<RSS_URL>
```

### 4.4 运行 Android 应用

1. 在 Android Studio 中，点击顶部工具栏的 **Run** 按钮（绿色 ▶️）
2. 选择刚才创建的模拟器
3. 等待编译和安装

---

## 五、真机调试

### 5.1 启用开发者选项

1. 打开手机 **设置**
2. 关于手机
3. 连续点击 **版本号** 7 次
4. 返回设置，找到 **开发者选项**
5. 启用 **USB 调试**

### 5.2 连接手机

1. 用 USB 数据线连接手机和电脑
2. 在手机上授权 USB 调试
3. 在 Android Studio 中选择连接的设备运行

### 5.3 配置网络

真机调试时，需要确保手机和电脑在同一局域网，并修改 API 地址：

```kotlin:android/app/src/main/kotlin/com/dreamecho/app/network/ApiService.kt:44
private const val BASE_URL = "http://<你的电脑IP>:8080/"
```

例如：
```kotlin
private const val BASE_URL = "http://192.168.1.100:8080/"
```

---

## 六、常见问题

### 6.1 Gradle 同步失败

**错误信息**：
```
Connection refused. Is the Gradle daemon running?
```

**解决方法**：
1. 点击 **File → Invalidate Caches / Restart**
2. 选择 **Invalidate and Restart**
3. 等待 Gradle 重新同步

### 6.2 SDK 版本不兼容

**错误信息**：
```
This project requires a newer version of the Android Gradle Plugin
```

**解决方法**：
1. 修改 `android/build.gradle.kts`:
```kotlin
plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.20" apply false
}
```

### 6.3 网络请求失败

**错误信息**：
```
java.net.ConnectException: Connection refused
```

**解决方法**：
1. 确保 Go 后端正在运行
2. 检查 IP 地址是否正确
3. 检查防火墙是否阻止了 8080 端口

### 6.4 模拟器无法启动

**解决方法**：
1. 确保 BIOS 中已启用 **Intel VT-x** 或 **AMD-V**
2. 在 Android Studio 中，点击 **Tools → SDK Manager → SDK Tools**
3. 取消勾选 **Intel x86 Emulator Accelerator**，然后重新勾选安装

---

## 七、项目功能

### 7.1 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 用户登录 | ✅ | 调用 Go 后端 `/api/login` |
| 播客列表 | ✅ | 显示热门播客 |
| 播客详情 | ✅ | 调用 Go 后端解析 RSS |
| 订阅管理 | ✅ | 订阅/取消订阅播客 |
| 搜索功能 | ✅ | 搜索播客 |
| 个人中心 | ✅ | 用户信息展示 |

### 7.2 待完善功能

| 功能 | 说明 |
|------|------|
| 音频播放 | 需要添加 MediaPlayer |
| 下载功能 | 需要本地存储 |
| 历史记录 | 需要本地数据库 |

---

## 八、截图预览

### 8.1 登录界面
```
┌─────────────────────────────┐
│                             │
│         🎙️ DreamEcho        │
│      听播客，上DreamEcho      │
│                             │
│  ┌───────────────────────┐  │
│  │ 用户名                 │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │ 密码                  │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │       登录             │  │
│  └───────────────────────┘  │
│                             │
└─────────────────────────────┘
```

### 8.2 首页
```
┌─────────────────────────────┐
│ DreamEcho           [退出]  │
├─────────────────────────────┤
│ 热门播客                      │
│ ┌─────┐ ┌─────┐ ┌─────┐    │
│ │ 内  │ │ Tea │ │ NPR │    │
│ │ 核  │ │ hour│ │     │    │
│ │ 恐  │ │     │ │     │    │
│ │ 慌  │ │     │ │     │    │
│ └─────┘ └─────┘ └─────┘    │
│                             │
│ 我的订阅                      │
│ ┌───────────────────────┐  │
│ │ 暂无订阅，去发现页面添加   │  │
│ └───────────────────────┘  │
└─────────────────────────────┘
```

---

## 九、相关链接

- **GitHub 项目**: https://github.com/Jeffyaoliang/podcast
- **Android Studio**: https://developer.android.com/studio
- **Kotlin 文档**: https://kotlinlang.org/docs/home.html
- **Jetpack Compose**: https://developer.android.com/jetpack-compose
- **Retrofit**: https://square.github.io/retrofit/

---

**有问题请随时问我！** 📱

